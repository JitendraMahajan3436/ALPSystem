@model REIN_MES_System.Models.MM_MTTUW_Shops
@using REIN_MES_System.App_LocalResources;

@{
    ViewBag.Title = "ConsumptionGraph";
    Layout = "~/Views/Shared/Admin/_Layout.cshtml";
}

<link href="~/Content/theme/plugins/select2/select2.min.css" rel="stylesheet" />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="col-md-12">

        <div class=" row">
            <div class="col-md-4">

                <div class="form-group">
                    <label>Shop Name *</label>
                    @*@Html.DropDownList("Shop_ID", Enumerable.Empty<SelectListItem>(), new { @class = "form-control select2", @style = "max-width:100%" })*@
                    @Html.DropDownListFor(model => model.Shop_ID, null, ResourceDisplayName.Select_Shop, htmlAttributes: new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.Shop_ID, "", new { @class = "text-danger oshop" })
                </div>
            </div>

            @*<div class=" row">
                <div class="box-footer">
                    <div class="form-group">
                        <input type="submit" value="Save" class="btn btn-primary" />
                        @Html.ActionLink("Show All", "Index", null, htmlAttributes: new { @class = "btn btn-linkedin" })
                        <br />
                    </div>
                </div>
                </div>*@
        </div>
    </div>
}


<script src="~/Content/theme/plugins/select2/select2.full.js"></script>
<link href="~/Content/theme/plugins/datepicker/datepicker3.css" rel="stylesheet" />


<script src="~/Content/theme/plugins/datepicker/bootstrap-datepicker.js"></script>
<script src="~/Content/theme/plugins/timepicker1/timepicker.js"></script>

<script type="text/javascript">

    $(document).ready(function (e) {

        $("#Shop_ID").change(function () {
            debugger;

            var Shop_Id = $("#Shop_ID").val();
            //var Plant = $("#Plant_ID").val();
            if (Shop_Id > 0) {

                //var options = {};
                //options.url = "/MeterwiseConsumptionGraph/Index";
                //options.type = "POST";
                //options.data = JSON.stringify({ Shop_ID: Shop_Id });
                //options.contentType = "application/json";
                //options.dataType = "json";

                var path = "/MeterwiseConsumptionGraph/Index?Shop_ID=" + Shop_Id;
                window.location.href = path;

            }
            else
            {
                if(Shop_Id==0)
                {
                    $('.oshop').html("Please Select Shop");
                }

            }

        })


 })
</script>