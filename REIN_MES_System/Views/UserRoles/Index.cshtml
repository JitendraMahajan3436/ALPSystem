@model IEnumerable<REIN_MES_System.Models.RS_User_Roles>
@using REIN_MES_System.App_LocalResources;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/admin/_Layout.cshtml";
}
<p class="position_absolute">
    @*@Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })*@
    <a href="@Url.Action("Create", "UserRoles")" class="btn btn-social btn-linkedin"><i class="fa fa-fw fa-plus-circle"></i> @ResourceGlobal.Create_New</a>
</p>
<table class="table table-bordered table-striped datatable_completes">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RS_Employee.Employee_No)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RS_Employee.Employee_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RS_Roles.Role_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RS_Roles.Role_Description)
            </th>
            <th>Actions</th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RS_Employee.Employee_No)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RS_Employee.Employee_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RS_Roles.Role_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RS_Roles.Role_Description)
            </td>
            <td>
                @*<a href="@Url.Action("Edit", "UserRoles", new { id = item.User_Role_Key })" class="col-md-1"><i class="fa fa-edit"></i></a>*@
                @*<a href="@Url.Action("Details", "UserRoles", new { id = item.User_Role_Key })" class="col-md-1"><i class="fa fa-fw fa-laptop"></i></a>*@
                @*@using (Html.BeginForm("Delete", "UserRoles", FormMethod.Post, new { @class = "delete-form col-md-1" }))
    {
        @Html.Hidden("id", item.User_Role_Key)
        @Html.AntiForgeryToken()
        <a href="" class="delete-btn"><i class="fa fa-trash"></i></a>
    }*@
                <a href="@Url.Action("Edit", "UserRoles", new { id = item.User_Role_Key })" class="col-md-1"><i class="fa fa-edit" style="color:blue"></i></a>
                <a href="@Url.Action("Details", "UserRoles", new { id = item.User_Role_Key })" class="col-md-1"><i class="fa fa-fw fa-laptop" style="color:blueviolet"></i></a> 
                <a href="@Url.Action("Delete", "UserRoles", new { id = item.User_Role_Key })" class="col-md-1"><i class="fa fa-trash" style="color:red"></i></a>
            </td>
        </tr>
    }

</table>

@section scripts{
    @Scripts.Render("~/Content/theme/plugins/datatables/js")
    @Scripts.Render("~/Scripts/user_datatable")
}
@section Styles{
   @Styles.Render("~/Content/theme/plugins/datatables/css")
}